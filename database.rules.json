{
  "rules": {
    ".read": true,
    ".write": true,
    "users": {
      "$userId": {
        ".write": "$userId === auth.id"
      },
      "restaurants": {
        ".write": true
      }
    }
  }
}
