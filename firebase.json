{
  "database": {
    "rules": "database.rules.json"
  },
  "storage": {
    "rules": "storage.rules.js"
  },
  "hosting": {
    "public": "build",
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  }
}
